#OPENAI_API_KEY = "Your API key can be enter here"
#is this for your demo purpose you can still it can be using ollama without openai api key
import os
from dotenv import load_dotenv

load_dotenv()
api_key = os.getenv("OPENAI_API_KEY")